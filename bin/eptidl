path = File.expand_path "../../", __FILE__

require "#{path}/lib/core"

ids = "19404" # decisions # 19404
type = "decision" 
Core.new(ids, type).download

# ids = "171693"
# type = "contract"
# Core.new(ids, type).download

exit

ids = "171693 226647 256176 224663 212837 2562236  197380"
type = "contract"
Core.new(ids, type).download

ids = "17956 20950 22057" # decisions
type = "decision" 
Core.new(ids, type).download